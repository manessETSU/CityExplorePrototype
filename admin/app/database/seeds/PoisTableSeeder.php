<?php

class PoisTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('pois')->truncate();

		$pois = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('pois')->insert($pois);
	}

}
